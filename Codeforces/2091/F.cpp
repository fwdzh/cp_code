// Date: 2025-03-26
// Time: 10:03:56
#include <bits/stdc++.h>
using namespace std;

void ChatGptDeepSeek()
{
    int n, m, d;
    cin >> n >> m >> d;
    vector<vector<int>> dp(n + 1, vector<int>(m + 1));
    
}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    int T = 1;
    cin >> T;
    while (T--)
        ChatGptDeepSeek();
    return 0;
}