// Date: 2025-03-12
// Time: 08:36:25

#include <bits/stdc++.h>
using namespace std;
using ll = long long;

void ChatGptDeepSeek()
{
    //看离得最近的人，
    //然后我们让除了它的小的数字，移动到x
    int n,m;
    ll k;
    cin>>n>>m>>k;
    vector<ll>t(n+1),y(n+1);
    vector<int>x(n+1);
    for(int i=1;i<=n;i++)
        cin>>t[i]>>x[i]>>y[i];
    

}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    int T = 1;
    cin >> T;
    while (T--)
        ChatGptDeepSeek();
    return 0;
}