#include<bits/stdc++.h>
using namespace std;
using pii = pair<int, int>;

void solve() {
    /*
     * 真是莫队板题吗？
     * 我好像看不出来是莫队的。。。
     * 知道是莫队好像也不会写来着
     * 子区间里每个字符出现的次数都为 k 的倍数
     * 没思路，明天再看看
     * /
}
int main() {
    cout << fixed << setprecision(12);
    ios::sync_with_stdio(false);
    cin.tie(nullptr), cout.tie(nullptr);
    int T = 1;
    // cin >> T;
    while(T--)
        solve();
    return 0;
}