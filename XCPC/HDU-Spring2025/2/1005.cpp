// 需要转动k/2 次 ，
// 如果k是一个奇数呢
// 比如转36度，转多少次都不会垂直

// Date: 2025-03-14
// Time: 19:47:13

#include <bits/stdc++.h>
using namespace std;

void ChatGptDeepSeek()
{
    int n, k;
    cin >> n >> k;
    if (k & 1) {
        cout << "0\n";
        return;
    }
    
    // 1+k/2
    // 1+k/

    

}

int main()
{
    ios::sync_with_stdio(false);
    cin.tie(nullptr);
    cout.tie(nullptr);
    int T = 1;
    cin >> T;
    while (T--)
        ChatGptDeepSeek();
    return 0;
}